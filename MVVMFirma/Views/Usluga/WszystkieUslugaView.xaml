<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieUslugaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdUslugi}"/>
                <DataGridTextColumn Header="Numer rezerwacji" Binding="{Binding IdRezerwacji}"/>
                <DataGridTextColumn Header="Typ usługi" Binding="{Binding TypUslugiNazwa}"/>
                <DataGridTextColumn Header="Data rozpoczęcia" Binding="{Binding DataRozpoczeciaUslugi}"/>
                <DataGridTextColumn Header="Data zakończenia" Binding="{Binding DataZakonczeniaUslugi}"/>
                <DataGridTextColumn Header="Kwota" Binding="{Binding Kwota}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
