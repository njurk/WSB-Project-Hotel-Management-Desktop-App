<UserControl x:Class="MVVMFirma.Views.RaportPrzychodowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="800">
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Przychody z rezerwacji w wybranym okresie" Margin="3" HorizontalContentAlignment="Center"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Od:" Margin="3"/>
            <DatePicker Grid.Column="1" Margin="3" SelectedDate="{Binding DataOd, ValidatesOnDataErrors=True}"/>
            <Label Grid.Column="2" Content="Do:" Margin="3"/>
            <DatePicker Grid.Column="3" Margin="3"  SelectedDate="{Binding DataDo, ValidatesOnDataErrors=True}"/>
        </Grid>
        <Button Grid.Row="2" Content="Oblicz" Margin="3" Command="{Binding ObliczPrzychodyCommand}"/>
        <Grid Grid.Row="3" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Przychód netto:" Margin="3"/>
            <TextBlock Grid.Column="1" Margin="3" Text="{Binding PrzychodNetto, StringFormat=C}"/>
            <Label Grid.Column="2" Content="Przychód brutto:" Margin="3"/>
            <TextBlock Grid.Column="3" Margin="3" Text="{Binding PrzychodBrutto, StringFormat=C}"/>
            <Label Grid.Column="4" Content="Podatek VAT:" Margin="3"/>
            <TextBlock Grid.Column="5" Margin="3" Text="{Binding PodatekVAT, StringFormat=C}"/>
        </Grid>
    </Grid>
</UserControl>
