<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieCennikView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="White">
        <DataGrid CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                
                <DataGridTextColumn Header="ID" Binding="{Binding IdCennika}"/>
                <DataGridTextColumn Header="Klasa" Binding="{Binding KlasaPokojuNazwa}"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding TypPokojuNazwa}"/>
                <DataGridTextColumn Header="Dorosły" Binding="{Binding CenaDorosly, StringFormat={}{0:C}}"/>
                <DataGridTextColumn Header="Dziecko" Binding="{Binding CenaDziecko, StringFormat={}{0:C}}"/>
                <DataGridTextColumn Header="Zwierzęta" Binding="{Binding CenaZwierzeta, StringFormat={}{0:C}}"/>
                
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
