<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePlatnoscView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdPlatnosci}"/>
                <DataGridTextColumn Header="Numer płatności" Binding="{Binding NrPlatnosci}"/>
                <DataGridTextColumn Header="Numer rezerwacji" Binding="{Binding IdRezerwacji}"/>
                <DataGridTextColumn Header="Sposób" Binding="{Binding SposobPlatnosciNazwa}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusPlatnosciNazwa}"/>
                <DataGridTextColumn Header="Data płatności" Binding="{Binding DataPlatnosci, StringFormat=yyyy-MM-dd}"/>
                <DataGridTextColumn Header="Kwota" Binding="{Binding Kwota, StringFormat={}{0:C}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
