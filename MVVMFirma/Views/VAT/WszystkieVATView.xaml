<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieVATView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views" xmlns:vm="clr-namespace:MVVMFirma.Helper"
                         mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <local:WszystkieViewBase.Resources>
        <vm:PercentConverter x:Key="PercentConverter"/>
    </local:WszystkieViewBase.Resources>

    <Grid Background="White">
        <DataGrid CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdVat}"/>
                <DataGridTextColumn Header="Stawka" Binding="{Binding Stawka, Converter={StaticResource PercentConverter}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
