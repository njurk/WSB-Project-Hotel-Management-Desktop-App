<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePokojView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid CanUserAddRows="False" IsReadOnly="True" CanUserDeleteRows="False" AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID pokoju" Binding="{Binding IdPokoju}"/>
                <DataGridTextColumn Header="Nr pokoju" Binding="{Binding NrPokoju}"/>
                <DataGridTextColumn Header="Typ" Binding="{Binding IdTypuPokojuNazwa}"/>
                <DataGridTextColumn Header="Klasa" Binding="{Binding IdKlasyPokojuNazwa}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IdStatusuPokojuNazwa}"/>
                <DataGridTextColumn Header="Piętro" Binding="{Binding IdPietraNazwa}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
